# Legacy implementations

`autopush/` contains the complete shell-based application that preceded
GitCerberus. It is retained only as behavioral reference for protected-branch
handling, debounce behavior, and the future automation engine.

Nothing in this directory is used by the GitCerberus build or runtime.
