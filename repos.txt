# List of local repository paths and their corresponding remotes.
# Each line must contain at least two fields separated by whitespace:
#
#   <absolute path to local repository>    <remote name>    [branch]    [log flag]
#
# The optional branch defaults to 'wip'.  The optional log flag can be one of
# 'log', '--log', 'debug', 'verbose', 'true' or '1' to enable verbose output.
#
# Lines starting with '#' are ignored.

# Example entries:
# /home/username/projects/repo1     origin      wip        --log
# /home/username/work/notes        upstream    backups
/home/eh930/bioinformatics/project/pancreas_remembers/script origin wip --log
/home/eh930/.custom origin wip
