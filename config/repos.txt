# List of local repository paths and their corresponding remotes.
# Each line must contain at least the absolute path to the local repository.
#
#   <absolute path to local repository>    [remote name]    [branch]    [log flag]
#
# The remote name is optional and defaults to 'origin' if omitted.
# The branch is optional and defaults to the current branch of the repository if omitted.
# The optional log flag can be one of 'log', '--log', 'debug', 'verbose', 'true' or '1' to enable verbose output.
#
# Lines starting with '#' are ignored.
#
# Example entries:
# /home/username/projects/repo1     origin      main        --log
# /home/username/work/notes        upstream    backups
# /home/username/justpath
/home/eh930/bioinformatics/project/pancreas_remembers/script
/home/eh930/.custom origin
/home/eh930/project/git-autopush
/home/eh930/project/cloud-mount-manager
